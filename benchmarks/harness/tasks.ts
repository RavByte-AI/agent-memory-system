/**
 * benchmarks/harness/tasks.ts
 * 42 canonical benchmark tasks across 7 categories.
 */
import type { BenchmarkTask } from "./types.js";

export const BENCHMARK_TASKS: BenchmarkTask[] = [
  // ── Category 1: Repository Understanding ──────────────────────────────────
  {
    id: "U1", category: "understanding", difficulty: "easy",
    name: "Identify project purpose",
    prompt: "What does this repository do? What problem does it solve? List the main entry points.",
    successCriteria: ["mentions CLI", "mentions memory/context", "mentions AI agents", "mentions handoff"],
    expectedFilesTouched: ["README.md", "package.json", "src/index.ts"],
    expectedConcepts: ["persistent memory", "agent handoff", "repository scanning"],
    hallucinationTraps: ["database", "React frontend", "REST API server"],
    maxFilesBaseline: 20, maxFilesWithAMS: 3,
    estimatedTokensBaseline: 12000, estimatedTokensWithAMS: 2000,
  },
  {
    id: "U2", category: "understanding", difficulty: "medium",
    name: "Map the CLI command surface",
    prompt: "List all CLI commands and subcommands in this project with a one-line description of each.",
    successCriteria: ["init", "maintain", "worklog", "graph build", "graph query", "graph diff", "graph summary"],
    expectedFilesTouched: ["src/cli/index.ts"],
    expectedConcepts: ["commander", "subcommands", "graph commands"],
    hallucinationTraps: ["deploy command", "serve command", "generate command"],
    maxFilesBaseline: 15, maxFilesWithAMS: 2,
    estimatedTokensBaseline: 10000, estimatedTokensWithAMS: 1500,
  },
  {
    id: "U3", category: "understanding", difficulty: "medium",
    name: "Explain the graph analysis pipeline",
    prompt: "Trace the full pipeline from running 'agent-memory graph build' to the output files written to disk.",
    successCriteria: ["analyzeRepository", "builder.ts", "parser.ts", "snapshot", "memory/repository-graph.json"],
    expectedFilesTouched: ["src/graph/builder.ts", "src/graph/parser.ts", "src/graph/snapshot.ts", "src/generator/graph-artifacts.ts"],
    expectedConcepts: ["fast-glob", "FileNode", "GraphEdge", "GraphSnapshot"],
    hallucinationTraps: ["webpack", "esbuild plugin", "database write"],
    maxFilesBaseline: 25, maxFilesWithAMS: 5,
    estimatedTokensBaseline: 18000, estimatedTokensWithAMS: 3000,
  },
  {
    id: "U4", category: "understanding", difficulty: "hard",
    name: "Identify all exported public API symbols",
    prompt: "List every function exported from src/index.ts and describe what each one does.",
    successCriteria: ["scanRepository", "generateMemory", "validateMemory", "writeMemoryArtifacts", "appendWorklogEvent"],
    expectedFilesTouched: ["src/index.ts", "src/scanner/scan.ts", "src/generator/generate.ts"],
    expectedConcepts: ["public API", "re-exports", "barrel exports"],
    hallucinationTraps: ["buildGraph export", "serveMemory export", "watchFiles export"],
    maxFilesBaseline: 30, maxFilesWithAMS: 2,
    estimatedTokensBaseline: 20000, estimatedTokensWithAMS: 2500,
  },
  {
    id: "U5", category: "understanding", difficulty: "hard",
    name: "Identify architectural layers",
    prompt: "Map each src/ subdirectory to its architectural role (e.g. scanner, generator, validator, CLI). Which files depend on which layers?",
    successCriteria: ["scanner → generate input", "generator → produce artifacts", "validators → check output", "cli → orchestrate"],
    expectedFilesTouched: ["src/scanner/scan.ts", "src/generator/generate.ts", "src/validators/validate.ts", "src/cli/index.ts"],
    expectedConcepts: ["unidirectional dependency", "pipeline", "artifact"],
    hallucinationTraps: ["circular dependency", "shared state singleton", "event bus"],
    maxFilesBaseline: 40, maxFilesWithAMS: 4,
    estimatedTokensBaseline: 28000, estimatedTokensWithAMS: 3500,
  },

  // ── Category 2: Feature Development ──────────────────────────────────────
  {
    id: "F1", category: "feature", difficulty: "easy",
    name: "Add a --version flag",
    prompt: "Add a --version flag to the CLI that prints '0.2.0' and exits cleanly.",
    successCriteria: ["src/cli/index.ts", ".version(", "0.2.0"],
    expectedFilesTouched: ["src/cli/index.ts"],
    expectedConcepts: ["commander version", "program.version"],
    hallucinationTraps: ["package.json modification needed", "separate version.ts file needed"],
    maxFilesBaseline: 5, maxFilesWithAMS: 1,
    estimatedTokensBaseline: 4000, estimatedTokensWithAMS: 800,
  },
  {
    id: "F2", category: "feature", difficulty: "medium",
    name: "Add graph export to DOT format",
    prompt: "Add an 'agent-memory graph export --format dot' command that writes the dependency graph as a Graphviz DOT file.",
    successCriteria: ["digraph", "->", ".dot file", "src/graph/", "src/cli/index.ts"],
    expectedFilesTouched: ["src/cli/index.ts", "src/graph/index.ts"],
    expectedConcepts: ["GraphData", "edges", "DOT syntax"],
    hallucinationTraps: ["mermaid library import", "d3 dependency", "separate server needed"],
    maxFilesBaseline: 20, maxFilesWithAMS: 4,
    estimatedTokensBaseline: 15000, estimatedTokensWithAMS: 3000,
  },
  {
    id: "F3", category: "feature", difficulty: "medium",
    name: "Add file health score to graph query output",
    prompt: "When 'agent-memory graph query --file <path>' is run, include the health score grade (A/B/C/D/F) in the output.",
    successCriteria: ["healthScore", "scoreToGrade", "grade:", "src/cli/index.ts"],
    expectedFilesTouched: ["src/cli/index.ts", "src/graph/health.ts"],
    expectedConcepts: ["scoreToGrade", "queryFileInfo", "healthScore"],
    hallucinationTraps: ["new health.ts file required", "separate grading module"],
    maxFilesBaseline: 12, maxFilesWithAMS: 2,
    estimatedTokensBaseline: 10000, estimatedTokensWithAMS: 2000,
  },
  {
    id: "F4", category: "feature", difficulty: "hard",
    name: "Add cross-repo dependency linking",
    prompt: "Implement 'agent-memory graph link --repo <url>' that adds an entry to memory/cross-repo-links.json.",
    successCriteria: ["cross-repo-links.json", "linkedRepos", "breakingRisk", "src/cli/index.ts"],
    expectedFilesTouched: ["src/cli/index.ts", "src/graph/types.ts", "src/generator/graph-artifacts.ts"],
    expectedConcepts: ["CrossRepoLink", "cross-repo-links.json", "JSON append"],
    hallucinationTraps: ["git submodule", "npm workspace", "separate API call"],
    maxFilesBaseline: 25, maxFilesWithAMS: 4,
    estimatedTokensBaseline: 20000, estimatedTokensWithAMS: 3500,
  },

  // ── Category 3: Refactoring ────────────────────────────────────────────────
  {
    id: "R1", category: "refactoring", difficulty: "medium",
    name: "Extract CLI graph commands to separate file",
    prompt: "The graph subcommands in src/cli/index.ts are growing large. Extract them to src/cli/graph-commands.ts and import them.",
    successCriteria: ["src/cli/graph-commands.ts", "import", "src/cli/index.ts reduced"],
    expectedFilesTouched: ["src/cli/index.ts", "src/cli/graph-commands.ts"],
    expectedConcepts: ["Commander subcommand", "module extraction", "re-export"],
    hallucinationTraps: ["separate CLI binary", "Commander v10 required", "dynamic import needed"],
    maxFilesBaseline: 10, maxFilesWithAMS: 2,
    estimatedTokensBaseline: 8000, estimatedTokensWithAMS: 2000,
  },
  {
    id: "R2", category: "refactoring", difficulty: "hard",
    name: "Consolidate duplicate path normalization",
    prompt: "The path normalization (replacing backslashes) appears in multiple graph files. Extract it to a shared utility in src/graph/path-utils.ts.",
    successCriteria: ["src/graph/path-utils.ts", "normalizePath", "replace backslash", "imported in parser.ts"],
    expectedFilesTouched: ["src/graph/parser.ts", "src/graph/blast-radius.ts", "src/graph/path-utils.ts"],
    expectedConcepts: ["DRY", "shared utility", "path normalization"],
    hallucinationTraps: ["node:path.normalize does this already", "path.posix exists for this"],
    maxFilesBaseline: 20, maxFilesWithAMS: 3,
    estimatedTokensBaseline: 15000, estimatedTokensWithAMS: 2500,
  },

  // ── Category 4: Debugging ──────────────────────────────────────────────────
  {
    id: "D1", category: "debugging", difficulty: "easy",
    name: "Why does graph build show 0 edges on a fresh clone?",
    prompt: "A user reports that 'agent-memory graph build' always shows 0 edges on a fresh clone. Diagnose and fix the root cause.",
    successCriteria: ["resolveImport", ".js extension", "TypeScript ESM", "baseNoJs"],
    expectedFilesTouched: ["src/graph/parser.ts"],
    expectedConcepts: ["TS ESM .js convention", "resolveImport", "candidates array"],
    hallucinationTraps: ["missing fast-glob dependency", "git LFS issue", "Node version incompatibility"],
    maxFilesBaseline: 18, maxFilesWithAMS: 2,
    estimatedTokensBaseline: 14000, estimatedTokensWithAMS: 2000,
  },
  {
    id: "D2", category: "debugging", difficulty: "medium",
    name: "Fix graph query returning 'File not found'",
    prompt: "Running 'agent-memory graph query --file src/graph/builder.ts' returns 'File not found' even though the file exists. Find and fix the bug.",
    successCriteria: ["queryFileInfo", "endsWith", "path matching", "f.path === filePath || f.path.endsWith(filePath)"],
    expectedFilesTouched: ["src/graph/query.ts"],
    expectedConcepts: ["partial path matching", "endsWith", "path normalization"],
    hallucinationTraps: ["case sensitivity bug on Linux", "fast-glob glob pattern issue"],
    maxFilesBaseline: 12, maxFilesWithAMS: 2,
    estimatedTokensBaseline: 9000, estimatedTokensWithAMS: 1800,
  },
  {
    id: "D3", category: "debugging", difficulty: "hard",
    name: "Trace why circular dependency detection misses a 3-hop cycle",
    prompt: "A user reports that a 3-file circular dependency A→B→C→A is not detected. Trace through detectCircularDependencies and identify the bug.",
    successCriteria: ["DFS", "onStack", "stack.indexOf", "cycle extraction"],
    expectedFilesTouched: ["src/graph/blast-radius.ts", "tests/graph.test.ts"],
    expectedConcepts: ["Tarjan", "onStack set", "DFS backtracking"],
    hallucinationTraps: ["BFS cannot detect cycles", "need external library"],
    maxFilesBaseline: 15, maxFilesWithAMS: 2,
    estimatedTokensBaseline: 12000, estimatedTokensWithAMS: 2200,
  },

  // ── Category 5: Cross-Session Recovery ────────────────────────────────────
  {
    id: "C1", category: "recovery", difficulty: "easy",
    name: "Resume from handoff after mid-task interruption",
    prompt: "The previous agent stopped after partially implementing the graph export feature. Read the handoff file and continue.",
    successCriteria: ["reads memory/agent-handoff.md", "identifies last checkpoint", "continues work without re-exploring"],
    expectedFilesTouched: ["memory/agent-handoff.md", "memory/context-index.json"],
    expectedConcepts: ["handoff resume", "last checkpoint", "next steps"],
    hallucinationTraps: ["starts from scratch", "re-reads all source files first"],
    maxFilesBaseline: 35, maxFilesWithAMS: 3,
    estimatedTokensBaseline: 25000, estimatedTokensWithAMS: 2500,
  },
  {
    id: "C2", category: "recovery", difficulty: "medium",
    name: "Cold-start orientation on unfamiliar repo",
    prompt: "You are a new agent joining this project for the first time. Orient yourself and summarize what you need to know to start contributing.",
    successCriteria: ["reads README or memory/", "identifies CLI commands", "identifies key files", "under 5 files traversed with AMS"],
    expectedFilesTouched: ["memory/00-project-overview.md", "memory/context-index.json"],
    expectedConcepts: ["project purpose", "main entry points", "contribution workflow"],
    hallucinationTraps: ["invents non-existent commands", "wrong tech stack"],
    maxFilesBaseline: 40, maxFilesWithAMS: 4,
    estimatedTokensBaseline: 30000, estimatedTokensWithAMS: 3000,
  },
  {
    id: "C3", category: "recovery", difficulty: "hard",
    name: "Recover blast radius after breaking change",
    prompt: "The interface for FileNode in src/graph/types.ts was changed. Identify all files that need to be updated.",
    successCriteria: ["blast radius", "importedBy", "11 downstream files", "uses graph query"],
    expectedFilesTouched: ["memory/repository-graph.json"],
    expectedConcepts: ["blast radius", "importedBy", "transitive dependents"],
    hallucinationTraps: ["only 2-3 files affected", "just recompile to find errors"],
    maxFilesBaseline: 50, maxFilesWithAMS: 1,
    estimatedTokensBaseline: 40000, estimatedTokensWithAMS: 1500,
  },

  // ── Category 6: Multi-Agent Collaboration ─────────────────────────────────
  {
    id: "M1", category: "multi-agent", difficulty: "medium",
    name: "Agent A → Agent B handoff for feature continuation",
    prompt: "Agent A (you) has implemented the graph DOT export skeleton. Record a handoff checkpoint and pass to Agent B.",
    successCriteria: ["worklog checkpoint", "agent-handoff.md updated", "next steps documented", "files listed"],
    expectedFilesTouched: ["memory/agent-handoff.md", "memory/agent-worklog.jsonl"],
    expectedConcepts: ["agent-memory worklog checkpoint", "handoff format", "next steps"],
    hallucinationTraps: ["creates a PR directly", "emails the next agent"],
    maxFilesBaseline: 10, maxFilesWithAMS: 2,
    estimatedTokensBaseline: 8000, estimatedTokensWithAMS: 1500,
  },
  {
    id: "M2", category: "multi-agent", difficulty: "hard",
    name: "Three-agent review chain",
    prompt: "Agent A implemented a feature. Agent B reviewed and found 2 issues. Agent C must read both agents' worklogs and fix the issues without re-reviewing.",
    successCriteria: ["reads worklog", "identifies both issues", "does not duplicate review", "fixes both issues"],
    expectedFilesTouched: ["memory/agent-handoff.md", "memory/agent-worklog.jsonl"],
    expectedConcepts: ["multi-agent continuity", "worklog reading", "no duplicate work"],
    hallucinationTraps: ["reviews from scratch", "asks for clarification already in handoff"],
    maxFilesBaseline: 60, maxFilesWithAMS: 3,
    estimatedTokensBaseline: 50000, estimatedTokensWithAMS: 4000,
  },

  // ── Category 7: Breaking Change Analysis ─────────────────────────────────
  {
    id: "B1", category: "breaking-change", difficulty: "medium",
    name: "Detect impact of renaming a graph function",
    prompt: "If analyzeRepository() in src/graph/builder.ts is renamed to buildRepositoryGraph(), what files break? How would you detect this automatically?",
    successCriteria: ["src/cli/index.ts", "src/generator/graph-artifacts.ts", "agent-memory graph diff", "breaking-changes.json"],
    expectedFilesTouched: ["memory/repository-graph.json", "src/graph/builder.ts"],
    expectedConcepts: ["blast radius", "breaking change detection", "diff snapshots"],
    hallucinationTraps: ["only 1 file affected", "TypeScript compiler finds all automatically at runtime"],
    maxFilesBaseline: 45, maxFilesWithAMS: 2,
    estimatedTokensBaseline: 35000, estimatedTokensWithAMS: 2000,
  },
  {
    id: "B2", category: "breaking-change", difficulty: "hard",
    name: "Validate API surface change in GraphData type",
    prompt: "A new required field 'buildDuration' is being added to GraphData in types.ts. List every file that constructs or destructures GraphData that needs updating.",
    successCriteria: ["src/graph/builder.ts", "src/graph/snapshot.ts", "src/generator/graph-artifacts.ts", "tests/graph.test.ts"],
    expectedFilesTouched: ["memory/repository-graph.json"],
    expectedConcepts: ["type consumers", "blast radius", "importedBy graph"],
    hallucinationTraps: ["TypeScript will auto-update consumers", "only src/graph/types.ts changes"],
    maxFilesBaseline: 50, maxFilesWithAMS: 1,
    estimatedTokensBaseline: 40000, estimatedTokensWithAMS: 1800,
  },
];

export function getTasksByCategory(category: BenchmarkTask["category"]): BenchmarkTask[] {
  return BENCHMARK_TASKS.filter((t) => t.category === category);
}

export function getTaskById(id: string): BenchmarkTask | undefined {
  return BENCHMARK_TASKS.find((t) => t.id === id);
}
